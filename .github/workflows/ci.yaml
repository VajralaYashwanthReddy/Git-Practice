name: Basic CI

on:
  pull:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name : Run build
        run : echo "CI is running".github/workflows/ci.yaml
